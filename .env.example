# Backend API base URL. Leave as /api in local dev if using Vite proxy.
# Set to the deployed backend URL in production, e.g.:
# VITE_API_BASE_URL=https://your-backend.example.com/api
VITE_API_BASE_URL=/api
